machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/node_modules"

  pre:
    - docker info && docker version
    - docker pull mongo
 
  override:
    - docker run --name mongo-test -d mongo
    - docker info
    - npm install -g grunt-cli
    - netstat -ntlap

test:
  override:
