machine:
  node:
    version: 0.12

  services:
    #- docker
    - mongodb

dependencies:
  cache_directories:
    - "~/sample_node_mongo/node_modules"

  pre:
    - docker info && docker version
 
  override:
    - npm install -g grunt-cli

test:
  override:
    - npm test

deploy:
