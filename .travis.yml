sudo: required

language: node_js

node_js:
  - 0.12

services:
  - docker

cache:
  directories:
    - node_modules

branches:
  only:
    - master

before_install:
  - "npm install -g grunt-cli webpack karma-cli"

install:
  - "npm test"
  - "npm install"

after_install:
  - echo "testing"
